# captured-state

An example that shows how to use `render_captured` and the resulting `Captured`
and `State` objects. Together these APIs can be used to render single blocks,
invoke macros, access exports of a template and more.

```console
$ cargo run
```

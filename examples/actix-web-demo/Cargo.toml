[package]
name = "actix-web-demo"
version = "0.1.0"
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
actix-web = "4.9.0"
minijinja = { path = "../../minijinja", features = ["loader"] }

[package]
name = "path-loader"
version = "0.1.0"
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
axominijinja = { path = "../../minijinja", features = ["loader"] }
once_cell = "1.13.1"
